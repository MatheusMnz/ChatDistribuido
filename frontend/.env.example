# Base URL for the REST API + Socket.IO.
# Leave EMPTY in Docker so requests use the same origin (nginx gateway):
#   REST -> /api , Socket.IO -> /socket.io
# For local `npm run dev`, it defaults to http://localhost:8080 if empty.
VITE_API_URL=
